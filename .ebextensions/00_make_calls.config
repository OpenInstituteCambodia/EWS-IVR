container_commands:
  01_make_calls:
    command: "cat .ebextensions/make_calls_cron_job.txt > /etc/cron.d/make_calls_cron_job && chmod 644 /etc/cron.d/make_calls_cron_job && sudo chmod -R 777 /var/log"
    leader_only: true

